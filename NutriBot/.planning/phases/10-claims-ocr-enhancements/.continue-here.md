---
phase: 10-claims-ocr-enhancements
task: 0
total_tasks: 0
status: complete
last_updated: 2026-02-18
---

<current_state>

Ad-hoc enhancements completed this session. No formal phase plan was in progress. All requested features are implemented and working.

</current_state>

<completed_work>

- **Claims due date**: Changed Monthly Claim Submission to MONTHLY:NEXT:10 (due 10th of following month). Added `app/recurrence.py` support for MONTHLY:NEXT:DD; updated preset.
- **Dashboard All Tasks grouping**: Grouped by category → template → tasks with expandable/collapsible hierarchy. Updated `app/routes.py` and `templates/dashboard.html`.
- **Multi-tag evidence**: One document can satisfy multiple requirements. Added `evidence_types_json` to EvidenceItem; checkboxes for evidence types on upload forms; updated `compute_missing_evidence`, `get_missing_requirement_types`, display logic.
- **OCR extraction for claim PDFs**: Added `app/ocr_extract.py` (PyMuPDF + pytesseract fallback), POST `/tasks/{id}/evidence/extract`, "Extract" button on task detail for PDFs. Dependencies: pymupdf, pytesseract, Pillow.
- **Idaho CNP PDF parsing**: Updated `parse_claim_text()` for Idaho NSLP Claim Summary format—total reimbursement, free/reduced/paid meals (comma-separated), breakfast totals, dates, adjustments.
- **Bug fixes**: `get_evidence_types` undefined in task_detail (added to context); `request.form.getlist` AttributeError (use `await request.form()` in async routes); `ensure_claim_fields` for Monthly Claim so Extract button appears.

</completed_work>

<remaining_work>

None. Session work is complete. User may want to:
- Test OCR extraction with more Idaho PDFs
- Add Tesseract OCR for scanned PDFs (optional; requires system install)
- Tune parsing if other claim form formats appear

</remaining_work>

<decisions_made>

- Used PyMuPDF (fitz) for PDF text extraction; pytesseract for OCR fallback when embedded text is minimal.
- Stored multi-tag as `evidence_types_json` (JSON array) with `evidence_type` as primary for backward compat.
- Idaho-specific parsing patterns first, then generic fallbacks.
- `ensure_claim_fields` called when loading Monthly Claim task so Structured Data/Extract always available.

</decisions_made>

<blockers>

None.

</blockers>

<context>

Post-v1.3 enhancements driven by user feedback. No formal roadmap phase. All features are additive and working. STATE.md shows v1.3 complete; next milestone not yet planned.

</context>

<next_action>

When resuming: Run `/gsd:new-milestone` to plan next milestone, or continue ad-hoc enhancements as needed. No pending tasks from this session.

</next_action>
