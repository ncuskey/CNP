---
phase: 09-page-migration-polish
plan: 07
type: execute
wave: 4
depends_on: [09-06]
files_modified:
  - templates/quick_capture.html
  - templates/quick_capture_success.html
  - app/routes.py
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Quick Note form submits via HTMX and shows success in-place without redirect"
    - "Quick Evidence form submits via HTMX and shows success in-place without redirect"
    - "Quick Log form submits via HTMX and shows success in-place without redirect"
  artifacts:
    - path: templates/quick_capture.html
      provides: "HTMX-enabled quick capture forms"
      contains: "hx-post"
    - path: app/routes.py
      provides: "HX-Request-aware quick handlers"
      pattern: "HX-Request|hx-request"
  key_links:
    - from: templates/quick_capture.html
      to: /quick/note
      via: hx-post
      pattern: "hx-post.*quick/note"
    - from: app/routes.py quick_note
      to: quick_capture_success.html
      via: "HX-Request check, TemplateResponse"
      pattern: "HX-Request|Hx-Request"
---

<objective>
Close the quick capture HTMX gap: add HTMX to all three quick capture forms (Note, Evidence, Log) and update backend to return partials instead of redirect when HX-Request is present.

Purpose: Achieve must-have "HTMX (checklist toggle, notes add, quick capture) works" — checklist and notes already use HTMX; quick capture was the only remaining gap.
Output: Quick capture forms submit via HTMX, show success message in-place, form resets; no full-page redirect.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-page-migration-polish/09-page-migration-polish-VERIFICATION.md
@templates/quick_capture.html
@templates/task_detail.html
@app/routes.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend HX-Request support for quick capture</name>
  <files>app/routes.py, templates/quick_capture_success.html</files>
  <action>
Create templates/quick_capture_success.html with a single parameterized partial:
- Accepts: message (str), task_id (int), anchor (str, e.g. "notes" or "evidence")
- Renders: `<div class="alert alert-success py-2" role="alert">{{ message }} <a href="/tasks/{{ task_id }}#{{ anchor }}">View task</a></div>`

Update app/routes.py for quick_note, quick_evidence, quick_log:
1. Add `request: Request` as first parameter to each handler.
2. After successful DB commit, check `request.headers.get("HX-Request")` (case-insensitive; "true" or any truthy value).
3. If HX-Request: return templates.TemplateResponse("quick_capture_success.html", {"request": request, "message": "...", "task_id": task_id, "anchor": "notes"|"evidence"}) with status 200. Set cookie on response via response.set_cookie(...).
4. If not HX-Request: keep existing RedirectResponse behavior (303 to /tasks/{id}#notes or #evidence).

Messages: "Note added." (quick_note), "Evidence attached." (quick_evidence), "Log added." (quick_log).
Anchors: quick_note and quick_log use "notes"; quick_evidence uses "evidence".

Preserve existing error handling (HTTPException 404, 400). Preserve cookie setting for last_task_id in both branches.
  </action>
  <verify>grep -E "HX-Request|Hx-Request" app/routes.py | head -5</verify>
  <done>Backend returns partial HTML when HX-Request header present; redirect when absent.</done>
</task>

<task type="auto">
  <name>Task 2: HTMX attributes on quick capture forms</name>
  <files>templates/quick_capture.html</files>
  <action>
Add HTMX to all three forms in templates/quick_capture.html. Each form needs:
- A feedback div above the form: `<div id="quick-{note|evidence|log}-feedback" class="mb-2"></div>`
- Form attributes: hx-post="{action}", hx-target="#quick-{note|evidence|log}-feedback", hx-swap="innerHTML"
- hx-on::after-request="this.reset()" to clear form after success (preserves fallback action/method for non-JS)
- Keep existing action, method, enctype (Quick Evidence keeps enctype="multipart/form-data" — HTMX sends multipart automatically when form has file input)

Quick Note: feedback div id="quick-note-feedback", hx-post="/quick/note"
Quick Evidence: feedback div id="quick-evidence-feedback", hx-post="/quick/evidence"
Quick Log: feedback div id="quick-log-feedback", hx-post="/quick/log"

Place feedback div immediately before each form's opening tag. Do not remove action/method — they provide fallback for no-JS.
  </action>
  <verify>grep -E "hx-post|hx-target|hx-swap" templates/quick_capture.html</verify>
  <done>All three forms have hx-post, hx-target, hx-swap; feedback divs exist; form resets on success.</done>
</task>

</tasks>

<verification>
1. Start app, visit /quick (or /today which includes quick_capture)
2. Submit Quick Note: select task, enter text, click Add Note — success message appears in-place, form clears, no redirect
3. Submit Quick Evidence: select task, choose file, set type/importance, click Attach — success message appears in-place, form clears, no redirect
4. Submit Quick Log: select task, enter text, click Log — success message appears in-place, form clears, no redirect
5. Verify fallback: disable JS, submit form — should still work via normal POST and redirect
</verification>

<success_criteria>
- Quick Note, Quick Evidence, Quick Log submit via HTMX
- Success feedback shown in-place (alert in feedback div)
- Form resets after successful submit
- No full-page redirect when HTMX is used
- Non-JS fallback preserved (action/method remain)
</success_criteria>

<output>
After completion, create `.planning/phases/09-page-migration-polish/09-07-SUMMARY.md`
</output>
